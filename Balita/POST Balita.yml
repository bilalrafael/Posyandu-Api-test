info:
  name: POST Balita
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/balita
  body:
    type: json
    data: |-
      {
        "nik": "3276010101019997",
        "nama_ortu": "Bilal Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
