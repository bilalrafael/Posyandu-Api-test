info:
  name: PUT Balita
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8000/api/balita/3
  body:
    type: json
    data: |-
      {
        "nama_ortu": "Bilal Rafael"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
