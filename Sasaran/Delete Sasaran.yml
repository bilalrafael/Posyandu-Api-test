info:
  name: Delete Sasaran
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8000/api/sasaran/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
