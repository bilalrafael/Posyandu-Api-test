info:
  name: GET Sasaran
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/sasaran
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
