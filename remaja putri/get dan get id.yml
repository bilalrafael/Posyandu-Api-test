info:
  name: get dan get id
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/remaja-putri/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
