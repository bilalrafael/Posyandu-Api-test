info:
  name: GET Remaja Putri
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/remaja-putri
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
