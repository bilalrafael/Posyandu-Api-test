info:
  name: POST Kecamatan
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/kecamatan
  body:
    type: json
    data: |-
      {
        "nama_kecamatan": "Kecamatan Pesantren"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
