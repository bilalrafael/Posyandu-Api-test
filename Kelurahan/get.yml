info:
  name: get
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8000/api/kelurahan/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
