info:
  name: post
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/api/kelurahan
  body:
    type: json
    data: |-
      {
      "nama_kelurahan": "Gadungan",
      "id_kecamatan": 7
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
