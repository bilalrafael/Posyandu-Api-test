info:
  name: POST Posyandu
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/posyandu
  body:
    type: json
    data: |-
      {
        "nama_posyandu": "Posyandu Dahlia",
        "id_kelurahan": 2,
        "id_puskesmas": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
