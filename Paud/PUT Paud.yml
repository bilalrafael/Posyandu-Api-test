info:
  name: PUT Paud
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8000/api/paud/2
  body:
    type: json
    data: |-
      {
        "nama_paud": "PAUD Dahlia Husada",
        "id_kelurahan": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
