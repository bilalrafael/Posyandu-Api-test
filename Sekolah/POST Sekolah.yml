info:
  name: POST Sekolah
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/sekolah
  body:
    type: json
    data: |-
      {
        "nama_sekolah": "SMP Negeri 1 Contoh",
        "id_kelurahan": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
