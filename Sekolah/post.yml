info:
  name: post
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/sekolah
  body:
    type: json
    data: |-
      {
        "nama_sekolah": "SMPN 1 Sehat",
        "id_kelurahan": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
